# astro-geo-backend

Backend utilities for resolving geographical locations and
astronomical reference data.

This project performs **geocoding and coordinate resolution**
and is intended to be used as a backend service.

No astrological or horoscope logic is implemented.

## Development

Create and activate a virtual environment:

    python3 -m venv .venv
    source .venv/bin/activate
    pip install -e .

